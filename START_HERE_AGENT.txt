═══════════════════════════════════════════════════════════════
  🚀 START HERE: Google Sheets AI Agent Setup
═══════════════════════════════════════════════════════════════

After starting your computer, follow these 3 simple steps:


┌─────────────────────────────────────────────────────────────┐
│ STEP 1: START SERVER & NGROK                                │
└─────────────────────────────────────────────────────────────┘

Double-click this file:
  → start_server_and_ngrok.bat

Wait for:
  ✓ Server running
  ✓ ngrok showing URL like: https://abc123.ngrok.io

COPY the ngrok HTTPS URL (the https:// one)


┌─────────────────────────────────────────────────────────────┐
│ STEP 2: UPDATE GOOGLE APPS SCRIPT                            │
└─────────────────────────────────────────────────────────────┘

1. Open your Google Sheet
2. Extensions → Apps Script
3. Find line 18: const API_URL = 'http://localhost:8000...'
4. Replace with: const API_URL = 'https://YOUR_NGROK_URL/api/agent/command';
   (Use the URL you copied from Step 1)
5. Save (Ctrl+S)


┌─────────────────────────────────────────────────────────────┐
│ STEP 3: UPDATE DIALOG HTML (ONE TIME ONLY)                   │
└─────────────────────────────────────────────────────────────┘

1. In Apps Script: + (Add file) → HTML → Name: "CommandDialog"
2. Open FIXED_COMMAND_DIALOG.html in your editor
3. Copy ALL content
4. Paste into CommandDialog.html in Apps Script
5. Save (Ctrl+S)


┌─────────────────────────────────────────────────────────────┐
│ DONE! USE IT NOW                                              │
└─────────────────────────────────────────────────────────────┘

1. Refresh Google Sheet
2. Menu appears: "🤖 AI Agent"
3. Click: "AI Agent → Open Command Dialog"
4. Try: "fix net profit function"


═══════════════════════════════════════════════════════════════
  💡 TIP: Keep the terminal window open!
═══════════════════════════════════════════════════════════════
